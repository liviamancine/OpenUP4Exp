/**
 * Keeps the current selection visible when the canvas container is resized,
 * provided the selection was already visible before the resize. If the selection
 * was out of view, this is a no-op.
 *
 */
export default class KeepSelectionVisible {
  static $inject: string[];

  /**
   * @param eventBus
   * @param canvas
   * @param selection
   */
  constructor(eventBus: import("../../core/EventBus").default, canvas: import("../../core/Canvas").default, selection: import("../selection/Selection").default);
}
//# sourceMappingURL=KeepSelectionVisible.d.ts.map